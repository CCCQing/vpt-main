RUN_N_TIMES: 1
DATA:
  NAME: "CUB"
  NUMBER_CLASSES: 200
  XLSA:
    PROTOCOL_MODE: "dev"
MODEL:
  TYPE: "vit"
  PROMPT:
    NUM_TOKENS: 16
    DROPOUT: 0.0

  SEMANTIC_BRANCH:
    USE_ANCHOR_FREE: True
    ANCHOR_TOKENS: 4
    FREE_TOKENS: 2
    FREE_COMPETE_LAMBDA: 0.0

  R_SIMILARITY:
    ENABLE: True
    USE_COSINE: True
    LOGIT_SCALE_INIT: 10.0

  ROLE_MIGRATION:
    ENABLE: False
    EARLY_END: 3
    LATE_START: 9

  CONSISTENCY:
    ENABLE: False
    PROJ: "linear"
    DIST: "cosine"

SOLVER:
  BASE_LR: 0.0005
  WEIGHT_DECAY: 0.0001
  WARMUP_EPOCH: 3
  TOTAL_EPOCH: 20
  LOSS: "softmax_margin_cm"
  LOSS_MARGIN: 0.00
  LOSS_CM_WEIGHT: 0.05
  LOSS_CONS_WEIGHT: 0.00
  LOSS_ANCHOR_CONS_WEIGHT: 0.00
  LOSS_FREE_KD_WEIGHT: 0.0
  OPTIMIZER: "adamw"
  SCHEDULER: "cosine"
  BIAS_MULTIPLIER: 1.0
  EVAL_MODE: "zsl"
  VIS:
    ENABLE: False
    EPOCH_LIST: [1, 3, 5, 7, 10, 15, 20]
    SPLITS: ["test_seen", "test_unseen"]
    MAX_SAMPLES: 8
    LOCAL_CONTROL: true
    ROLLOUT: true
    GT_HN_COMPARE: true
    TRENDS: true
